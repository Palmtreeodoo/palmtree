<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_cron_auto_post_draft_entry" model="ir.cron">
        <field name="name">Account; Insert Record of GSTR-1 into temp table</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="nextcall" eval="(DateTime.now().replace(hour=18, minute=29) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')" />
        <field name="doall" eval="False"/>
        <field name="model_id" ref="model_l10n_in_account_invoice_report_temp"/>
        <field name="code">model.update_data_from_sql_view()</field>
        <field name="state">code</field>
    </record>
</odoo>
